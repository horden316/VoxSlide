services:
  qwen-tts:
    build:
      context: ./qwen_tts_service
    environment:
      - QWEN_TTS_MODEL=${QWEN_TTS_MODEL:-Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice}
      - QWEN_TTS_DEVICE=${QWEN_TTS_DEVICE:-cuda:0}
      - QWEN_TTS_ATTN_IMPLEMENTATION=${QWEN_TTS_ATTN_IMPLEMENTATION:-sdpa}
      - QWEN_TTS_INSTRUCT=${QWEN_TTS_INSTRUCT:-Speak in a neutral, consistent, clear voice.}
      - QWEN_TTS_SEED=${QWEN_TTS_SEED:-316}
      - QWEN_TTS_DO_SAMPLE=${QWEN_TTS_DO_SAMPLE:-false}
      - QWEN_TTS_TEMPERATURE=${QWEN_TTS_TEMPERATURE:-0.7}
      - QWEN_TTS_SUBTALKER_DOSAMPLE=${QWEN_TTS_SUBTALKER_DOSAMPLE:-false}
      - QWEN_TTS_SUBTALKER_TEMPERATURE=${QWEN_TTS_SUBTALKER_TEMPERATURE:-0.7}
      - QWEN_TTS_MAX_CHARS_PER_CHUNK=${QWEN_TTS_MAX_CHARS_PER_CHUNK:-120}
      - QWEN_TTS_CHUNK_SILENCE_MS=${QWEN_TTS_CHUNK_SILENCE_MS:-300}
      - QWEN_TTS_MAX_BATCH_CHUNKS=${QWEN_TTS_MAX_BATCH_CHUNKS:-3}
    volumes:
      - ./storage/models:/models
    gpus: all
    ports:
      - "7860:7860"

  backend:
    build:
      context: ./backend
    env_file:
      - path: .env
        required: false
    environment:
      - DATABASE_URL=sqlite:////app/storage/app.db
      - STORAGE_DIR=/app/storage
      - BACKEND_CORS_ORIGINS=${BACKEND_CORS_ORIGINS:-*}
      - TTS_PROVIDER=${TTS_PROVIDER:-qwen_local}
      - QWEN_TTS_ENDPOINT=${QWEN_TTS_ENDPOINT:-http://qwen-tts:7860/tts}
      - QWEN_TTS_MODEL=${QWEN_TTS_MODEL:-Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice}
      - QWEN_TTS_VOICE=${QWEN_TTS_VOICE:-Ryan}
      - QWEN_TTS_VOICES=${QWEN_TTS_VOICES:-Ryan:Ryan,Aiden:Aiden,Vivian:Vivian,Serena:Serena,Uncle_Fu:Uncle Fu,Dylan:Dylan,Eric:Eric,Ono_Anna:Ono Anna,Sohee:Sohee}
      - OPENAI_TTS_MODEL=${OPENAI_TTS_MODEL:-gpt-4o-mini-tts}
      - OPENAI_TTS_VOICE=${OPENAI_TTS_VOICE:-alloy}
      - VIDEO_ENCODER=${VIDEO_ENCODER:-h264_nvenc}
      - VIDEO_SEGMENT_WORKERS=${VIDEO_SEGMENT_WORKERS:-4}
    volumes:
      - ./storage:/app/storage
    gpus: all
    ports:
      - "8000:8000"
    depends_on:
      - qwen-tts

  frontend:
    build:
      context: ./frontend
    environment:
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8000}
    ports:
      - "3000:3000"
    depends_on:
      - backend
