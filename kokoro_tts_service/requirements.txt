fastapi==0.115.6
uvicorn[standard]==0.34.0
kokoro==0.9.4
soundfile==0.14.0
numpy==2.4.6
# CUDA build from PyPI (same pin as the Qwen service) so the idle RTX 4000
# can serve synthesis; KOKORO_TTS_DEVICE=cpu still works with this wheel.
torch==2.13.0
