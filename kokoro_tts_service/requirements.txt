# torch is installed separately in the Dockerfile from the CPU wheel index;
# the 82M model runs faster than realtime on CPU and skipping the CUDA wheel
# keeps the image ~5GB smaller.
fastapi==0.115.6
uvicorn[standard]==0.34.0
kokoro==0.9.4
soundfile==0.14.0
numpy==2.4.6
